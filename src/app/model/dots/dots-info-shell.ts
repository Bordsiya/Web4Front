export class DotsInfoShell {
    x: Array<number>;
    y: number;
    r: Array<number>;

    constructor(x: Array<number>, y: number, r: Array<number>) {
        this.x = x;
        this.y = y;
        this.r = r;
    }
}
